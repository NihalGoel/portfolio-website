---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectGrid from '../../components/gallery/ProjectGrid.astro';
import { getCollection } from 'astro:content';

const projects = await getCollection('projects');
---

<BaseLayout title="Work — Elena Vasquez">
  <section class="work-page container">
    <header class="page-header">
      <h1>Work</h1>
      <p>Installations, sculpture, photography, and mixed media</p>
    </header>
    <ProjectGrid projects={projects} />
  </section>
</BaseLayout>

<style>
  .work-page {
    padding: 8rem 0 4rem;
  }

  .page-header {
    margin-bottom: 2rem;
  }

  .page-header h1 {
    margin-bottom: 0.75rem;
  }

  .page-header p {
    font-size: 1rem;
    opacity: 0.5;
  }
</style>
